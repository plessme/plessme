package com.bongladesch.plessme.users.usecase;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class TestNativeCreateUser extends TestCreateUser {
    // Execute the same tests but in native mode.
}