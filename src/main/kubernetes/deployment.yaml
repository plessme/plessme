apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: plessme
  name: plessme-dpl
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: plessme
  strategy:
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: plessme
    spec:
      containers:
      - image: ingress-nginx-svc.kube-system.svc.bongladesch.com/docker-dev/pless-me
        name: pless-me
        resources:
          limits:
            cpu: "1"
            memory: 512Mi
          requests:
            cpu: "0.1"
            memory: 124Mi
